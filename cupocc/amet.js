     const config = {
         cacheNavPersist: true,
         anotherSetting: 'value'
     };
     
     if (config.cacheNavPersist) {
         // Perform actions related to navigation persistence
     }
     